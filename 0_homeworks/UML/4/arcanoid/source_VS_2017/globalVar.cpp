#include "precompiledHeaders.h"

_settingsGame settings;

const HANDLE HANDLE_INPUT = GetStdHandle(STD_INPUT_HANDLE);
const HANDLE HANDLE_OUTPUT = GetStdHandle(STD_OUTPUT_HANDLE);

LOG_GAME gameLog(10000);


